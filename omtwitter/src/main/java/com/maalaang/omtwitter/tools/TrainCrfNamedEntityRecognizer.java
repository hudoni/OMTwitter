package com.maalaang.omtwitter.tools;

public class TrainCrfNamedEntityRecognizer {
	/*
		
			DBPediaEntityCRFClassifier classifier = new DBPediaEntityCRFClassifier()
			
//			String[] trainingFiles = new String[nfold-1]
			String[] trainingFiles = new String[nfold]
			for (int i = 0; i < nfold; i++) {
				trainingFiles[i] = String.format("%s.fold.%02d", trainingset, i + 1);
			
			
//			classifier.train(trainingFiles, modelfile, stanfodPosTaggerModel, null, false);

*/
}
